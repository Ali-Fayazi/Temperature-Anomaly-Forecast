# 🌡️ Temperature Anomaly Forecast for Energy Risk Assessment

## چکیده پروژه: تحلیل سری زمانی جهت بهینه‌سازی انرژی
این پروژه علم داده به عنوان دمو برای سیستم "EnergySense" توسعه یافته است. هدف اصلی، پیش‌بینی دقیق **ناهنجاری‌های دمایی جهانی** تا ۱۲ سال آینده (از ۲۰۱۶ تا ۲۰۲۸) با استفاده از مدل‌های سری زمانی است. این تحلیل، داده‌های حیاتی برای برنامه‌ریزی زیرساخت‌های انرژی و مدیریت ریسک‌های ناشی از تغییرات اقلیمی طولانی‌مدت را فراهم می‌کند.

---

## 💻 روش‌شناسی فنی و مدل‌سازی

### ۱. پایپ‌لاین تحلیل و قدرت مدل
پروژه از یک پایپ‌لاین استاندارد علم داده با استفاده از **مدل Prophet (توسعه یافته توسط فیس‌بوک)** پیروی می‌کند:

* **پیش‌پردازش (Data Preprocessing):** داده‌های خام (۱۹۸۰-۲۰۱۶) تمیزکاری و برای مدل Prophet (ستون‌های `ds` و `y`) استاندارد شدند.
* **مدل‌سازی (Modeling):** مدل Prophet با پارامتر `changepoint_prior_scale` روی مقدار پایین **۰.۰۱** تنظیم گردید تا پایداری بیشتری در برابر نویز و داده‌های پرت داشته باشد.

### ۲. اعتبارسنجی (Validation)
| معیار | توضیح حرفه‌ای |
| :--- | :--- |
| **دوره پیش‌بینی** | ۱۲ سال کامل (از ۲۰۱۶ تا ۲۰۲۸). |
| **روش ارزیابی** | دقت مدل با محاسبه **میانگین خطای مطلق (MAE)** روی داده‌های تاریخی سنجیده شده است. |
| **نتیجه اعتبار** | مدل به خطای بسیار کم **MAE = 0.1152 °C** دست یافته است. این نتیجه نشان می‌دهد که میانگین خطای پیش‌بینی مدل تنها ۰.۱۱ درجه سانتی‌گراد بوده که برای پیش‌بینی بلندمدت، بسیار قابل اعتماد است. |

---

## 📊 نتایج کلیدی و تأثیر پروژه

نتایج بصری مدل، دو بینش حیاتی را نشان می‌دهد:

1.  **شتاب گرمایش (Trend Acceleration):** روند دمایی پس از سال‌های ۱۹۶۸ به صورت قابل توجهی شتاب گرفته و این روند صعودی در دوره پیش‌بینی ادامه می‌یابد.
    * **نمودار تحلیل مؤلفه‌ها:** 
2.  **پیک فصلی (Seasonal Peaks):** بالاترین ناهنجاری‌های دمایی به طور سیستماتیک در ماه‌های پایانی سال (اواخر مهر/آبان) رخ می‌دهد. این الگو برای برنامه‌ریزی تقاضای انرژی در پاییز بسیار حیاتی است.
    * **نمودار پیش‌بینی نهایی:** 

---

## ⚙️ نحوه اجرا و خروجی (Execution & Output)

این پروژه به صورت یک اسکریپت قابل حمل پایتون طراحی شده است که خروجی‌های بصری و داده‌ای را تولید می‌کند.

### ۱. پیش‌نیازها
برای اجرای پروژه، کتابخانه‌های زیر را نصب کنید (فایل `requirements.txt` را برای نصب خودکار استفاده کنید):

```bash
pip install -r requirements.txt

### ۲. اجرای اسکریپت
پس از نصب وابستگی‌ها و قرار دادن فایل داده (`.csv`) در پوشه اصلی، دستور زیر را در ترمینال اجرا کنید:

```bash
python Temperature_Anomaly.py